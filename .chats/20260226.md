# Instructions

## Build workspace and S3 service from scratch

make this a workspace that has ./crates (for common crates) and ./apps (for each localstack app), all deps should be put in workspace cargo and crates/apps cargo should use xxx = { workspace = true }. submodule localstack to ./vendors/localstack. Then based on ./specs, think ultra hard and build an extensive system that could gradually build core services for local stack. And only build s3 for now. Make sure s3 functionalities reach feature parity and can pass all tests. Do not enter plan mode.

## Dependency versioning preference

do not use ~ to lock version, always use latest deps

## Commit the code

commit the code

## Design GitHub Action for S3 as local container

now think about how to make it a gh action so that other repo could use this as a local container in CI (localhost:4566). reference the ./vendors code. Explain me your solution. Do nothing yet.

go ahead

## Build integration tests using GitHub Action

can you use it in github workflow and see how to do thorough integration test using tyrchen/localstack-rs@v1? See how ./vendors is doing this and see if we can borrow the test suite (or rewrite it). Explain me your solutions.

Go ahead!

## Push and monitor CI

can you push and monitor how CI works

## Reconsider using Smithy instead of s3s

wait can you not use s3s but build the service through smithy?

## Full rebuild based on Smithy spec

go ahead build based on this spec entirely phase by phase, commit after each phase. Then do a thorough code review against the spec and fix all valid issues. Then verify by running all integration tests and 3rd party test suite and fix issues accordingly. If all validated, push

## Rename project to avoid brand risk

Once you finished everything, think about one thing: does calling this codebase localstack-rs have brand risk? localstack seems to go with business, should we change to different name? Think ultra hard and explain only. Do nothing.

ok let's make this change and push

## Update README

now based on the code base update README.md
